import { Meta, Preview, Props, Story } from '@not-govuk/docs-components';
import { Header } from '../src/Header';
import readMe from '../README.md';

<Meta
  title="Header"
  component={ Header }
  parameters={ {
    chromatic: { viewports: [800, 600] },
    description: 'HODS\' page header.',
    jest: ['Header'],
    notes: readMe
  } }
/>

# Header

HODS' page header.

<Preview withToolbar>
  <Story name="Header">
    <Header serviceName="Service name" />
  </Story>
</Preview>

<Props of={ Header } />


## Stories
### Standard

A standard Header.

<Preview>
  <Story name="Standard">
    <Header />
  </Story>
</Preview>


### Service title

A header with a service title.

<Preview>
  <Story name="Service title">
    <Header serviceName="Service name" />
  </Story>
</Preview>


### Sign-out

A header with an option to sign out.

<Preview>
  <Story name="Sign-out">
    <Header
      accountHref="#/my-account"
      serviceName="Service name"
      signOutHref="#/auth/sign-out"
      username="User name"
    />
  </Story>
</Preview>


### Navigation

A header with navigation links.

<Preview>
  <Story name="Navigation">
    <Header
      accountHref="#/my-account"
      navigation={[
        { href: '/styles', text: 'Styles' },
        { href: '/components', text: 'Components' },
        { href: '/patterns', text: 'Patterns' },
        { href: '/resources', text: 'Resources' },
        { href: '/get-involved', text: 'Get involved' }
      ]}
      serviceName="Service name"
      signOutHref="#/auth/sign-out"
      username="User name"
    />
  </Story>
</Preview>